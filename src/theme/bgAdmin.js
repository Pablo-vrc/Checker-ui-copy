import bgBody from "assets/img/background-body-admin.png";

export const bgAdmin = {
  styles: {
    global: (props) => ({
      body: {
        bg: "rgba(0, 0, 0, 0.80)",
        bgSize: "cover",
        bgPosition: "center center",
      },
    }),
  },
};
